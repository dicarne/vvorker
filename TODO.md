# TODO

## 分布式云

需要实现的目标：多个agent节点提供服务，并agent可以逐个更新以避免服务中断。master节点可以随时停止，不影响agent提供服务。

[ ] agent为无状态节点，不要手动设置节点名称，随机名称，默认提供所有服务
[ ] 定时器由主节点控制，agent节点将不再启动定时器
[ ] 确认主节点中断服务后不影响agent节点正常服务（依赖数据库的服务除外）
[ ] 主节点恢复服务后，agent节点将自动恢复连接
[ ] 子节点提供一种直接连接数据库的模式，避免依赖主节点
[ ] 主节点的控制台将由子节点代理